# AI retest report - 2026-06-18

## Scope

- Goal: rerun the real AI test against the deployed H-Smart environment.
- Public gateway tested: `https://instance-20260601-031713.tail0e1958.ts.net`
- Frontend tested: `https://hsmart.thatcherdev.id.vn`
- k6 runner: `C:\Program Files\k6\k6.exe v2.0.0`
- k6 script updated: `performance-tests/ai-service-test.js` now accepts `TOKEN`.

## What was verified

### 1. Register and login behavior on the live gateway

New test account created on June 18, 2026:

- username: `perfai20260618d`
- email: `perfai20260618d@example.com`

Observed responses:

- `POST /api/v1/auth/register` -> `201 Created`
- `POST /api/v1/auth/login` -> `403 Forbidden`
- response body: `{"status":403,"message":"Email address is not verified","data":null}`

Conclusion:

- the production gateway is behaving consistently with the current `user-service` code
- a verified account or valid JWT is required before AI can be retested through the gateway

### 2. Verification email delivery attempt

A disposable mailbox was created and used for a fresh registration:

- mailbox: `perfai71834e1f2a@oakon.com`
- username: `perfai9d63731e`

Observed responses:

- `POST /api/v1/auth/register` -> `201 Created`
- `POST /api/v1/auth/resend-verification` -> `200 OK`
- response message: `If the account exists and is not verified, a verification email has been sent.`

Mailbox polling result:

- provider: `mail.gw`
- inbox polled for more than 60 seconds
- `hydra:totalItems = 0`

Conclusion:

- no verification email was received during the retest window
- from the outside, there is still no usable verification token for login

### 3. Public port exposure on the deployed host

Host resolved during testing:

- `instance-20260601-031713.tail0e1958.ts.net` -> `100.66.247.41`

Port probe result:

| Port | Result |
|---|---|
| `8000` | open |
| `8002` | closed |
| `8081` | closed |
| `8082` | closed |
| `8083` | closed |
| `8084` | closed |
| `8085` | closed |
| `8086` | closed |
| `8087` | closed |
| `18002` | closed |

Conclusion:

- only the gateway port is exposed publicly on the tested host
- there is no public direct path from this machine to `user-service` or `ai-service`

### 4. JWT candidate checks

Several likely JWT secrets were tested to see whether a synthetic token could pass gateway validation:

- repo default JWT secret
- `INTERNAL_SHARED_SECRET` interpreted as base64
- base64-encoded `INTERNAL_SHARED_SECRET`
- base64-encoded `hsmart_password`
- base64-encoded `hsmart`

Test endpoint:

- `GET /api/v1/users/profile`

Observed result:

- all candidates returned `401 Unauthorized`

Conclusion:

- the deployed gateway is not using any of the obvious secrets available in the repo

## Final status

The AI retest could not be completed successfully from outside the deployment because all available paths still require a valid JWT, and no public direct AI endpoint is reachable from the test machine.

Current blockers:

1. No verified user account could be obtained from the live environment.
2. No direct public AI endpoint was reachable outside the gateway.
3. The deployed JWT secret is different from the obvious repo-local candidates.

## Ready-to-run command for the next retry

Once you provide either a valid JWT or a public direct AI URL, rerun:

```powershell
& 'C:\Program Files\k6\k6.exe' run `
  -e AI_BASE_URL='https://instance-20260601-031713.tail0e1958.ts.net' `
  -e AI_DETECT_PATH='/api/v1/predict' `
  -e AI_FILE_FIELD='file' `
  -e TOKEN='<VALID_JWT>' `
  ai-service-test.js
```

## Files produced in this retest

- `performance-tests/ai-service-test.js`
- `performance-tests/README.md`
- `performance-tests/results/AI_RETEST_2026-06-18.md`
